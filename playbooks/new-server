---
- name: "Setup new Server (admin / ssh)"
  hosts: new
  become: true
  remote_user: "{{default_admin}}"
  roles:
  - update
  - role: set-admin
    vars: 
     ansible_become_pass: "{{ admin_pwd}}"
